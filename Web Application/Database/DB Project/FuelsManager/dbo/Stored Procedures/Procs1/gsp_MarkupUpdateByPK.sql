CREATE PROCEDURE [dbo].[gsp_MarkupUpdateByPK]
(
		@MarkupGuid uniqueidentifier
	,	@PurchasingEntity nvarchar(50)=NULL
	,	@MarkupRate float=NULL
	,	@SOFA_FEA bit=NULL
	,	@Quantities float=NULL
	,	@UpdatedBy udtUserID=NULL
	,	@UpdatedDate datetimeoffset(7)=NULL
	,	@LookupServiceTypeIndex int=NULL
	,	@_RowVersion timestamp=NULL OUTPUT
	,	@NullOverridePurchasingEntity BIT=0 
	,	@NullOverrideMarkupRate BIT=0 
	,	@NullOverrideSOFA_FEA BIT=0 
	,	@NullOverrideQuantities BIT=0 
	,	@NullOverrideUpdatedDate BIT=0 
	,	@NullOverrideLookupServiceTypeIndex BIT=0 
)
	AS
	BEGIN
	------------------------------------------------------------------------------------------------------
	-- Stored procedure: [dbo].[gsp_MarkupUpdateByPK] 
	-- Author: DBA - Auto generated
	-- Version/Date: 1.0.003 / 2014-02-05 16:52:29.5584030 -05:00
	-- Purpose: Update table [dbo].[tblMarkup]
	-- Notes:
	-- 1. @MarkupGuid and @UpdatedBy are required parameter.
	-- 2. If a value other than NULL is passed on @_RowVersion parameter then the stored procedure verifies whether _RowVersion of the record matches with the  
	--    @_RowVersion parameter and it will throw an exception if they don't match, otherwise it saves the parameters regardless.
	-- 3. The @_RowVersion output parameter will always be updated with new timestamp generated by the updating of the record.
	-- 4. To update a column with NULL then set the corresponding "@NullOverride..." parameter to 1 and either pass NULL through the correlated parameter 
	--    or do not include the parameter at all. 
	--    Example - Saving NULL to SiteGuid on tblEquipment:
	--            EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...',@SiteGuid=NULL, @NullOverrideSiteGuid=1 
	--       or   EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...', @NullOverrideSiteGuid=1 
	------------------------------------------------------------------------------------------------------
	SET NOCOUNT ON;
	BEGIN TRY
		IF @_RowVersion IS NOT NULL AND NOT EXISTS(SELECT 1 FROM [dbo].[tblMarkup] WHERE MarkupGuid=@MarkupGuid AND _RowVersion=@_RowVersion)
		BEGIN
			RAISERROR('Attempted to modify a stale copy of the record',16,1);
			RETURN;
		END
 
		UPDATE [dbo].[tblMarkup] SET
			[PurchasingEntity]=(CASE ISNULL(@NullOverridePurchasingEntity,0) WHEN 1 THEN @PurchasingEntity ELSE ISNULL(@PurchasingEntity,[PurchasingEntity]) END)
		,	[MarkupRate]=(CASE ISNULL(@NullOverrideMarkupRate,0) WHEN 1 THEN @MarkupRate ELSE ISNULL(@MarkupRate,[MarkupRate]) END)
		,	[SOFA_FEA]=(CASE ISNULL(@NullOverrideSOFA_FEA,0) WHEN 1 THEN @SOFA_FEA ELSE ISNULL(@SOFA_FEA,[SOFA_FEA]) END)
		,	[Quantities]=(CASE ISNULL(@NullOverrideQuantities,0) WHEN 1 THEN @Quantities ELSE ISNULL(@Quantities,[Quantities]) END)
		,	[UpdatedBy]= ISNULL(@UpdatedBy,SUSER_SNAME())
		,	[UpdatedDate]=ISNULL(@UpdatedDate,SYSDATETIMEOFFSET())
		,	[LookupServiceTypeIndex]=(CASE ISNULL(@NullOverrideLookupServiceTypeIndex,0) WHEN 1 THEN @LookupServiceTypeIndex ELSE ISNULL(@LookupServiceTypeIndex,[LookupServiceTypeIndex]) END)
		WHERE	MarkupGuid=@MarkupGuid;
 
		SELECT @_RowVersion=_RowVersion        
		FROM [dbo].[tblMarkup]           
		WHERE MarkupGuid=@MarkupGuid;
	
 
	END TRY
	BEGIN CATCH        
		DECLARE	@_ErrMessage NVARCHAR(2048)      
				, @_ErrNumber INT           
				, @_ErrProcName NVARCHAR(126)           
				, @_ErrLineNumber INT;            
		SET @_ErrMessage = ERROR_MESSAGE();        
		SET @_ErrNumber = ERROR_NUMBER();        
		SET @_ErrProcName= ERROR_PROCEDURE();        
		SET @_ErrLineNumber = ERROR_LINE();            
		SET @_ErrMessage = 'Error: ' + @_ErrMessage + CHAR(13)+CHAR(10)                 
						+ 'Number: ' + CAST(@_ErrNumber AS VARCHAR(20)) + CHAR(13)+CHAR(10)                 
						+ 'Procedure Name: gsp_MarkupUpdateByPK' + CHAR(13)+CHAR(10)                  
						+ 'Line Number: ' + ISNULL(CAST(@_ErrLineNumber AS VARCHAR(20)),'') + CHAR(13)+CHAR(10);         
		RAISERROR(@_ErrMessage,18,1);      
	END CATCH    
END     
	
