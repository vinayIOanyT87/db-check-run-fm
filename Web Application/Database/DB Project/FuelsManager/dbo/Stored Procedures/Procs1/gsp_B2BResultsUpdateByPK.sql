CREATE PROCEDURE [dbo].[gsp_B2BResultsUpdateByPK]
(
		@B2BResultGuid uniqueidentifier
	,	@ResultsID int=NULL
	,	@TransID nvarchar(64)=NULL
	,	@Type char=NULL
	,	@Message nvarchar(100)=NULL
	,	@DataError nvarchar(15)=NULL
	,	@ErrorStatus int=NULL
	,	@Disputed int=NULL
	,	@Corrected int=NULL
	,	@ReceivedSentDate datetimeoffset(7)=NULL
	,	@_RowVersion timestamp=NULL OUTPUT
	,	@NullOverrideResultsID BIT=0 
	,	@NullOverrideTransID BIT=0 
	,	@NullOverrideType BIT=0 
	,	@NullOverrideMessage BIT=0 
	,	@NullOverrideDataError BIT=0 
	,	@NullOverrideErrorStatus BIT=0 
	,	@NullOverrideDisputed BIT=0 
	,	@NullOverrideCorrected BIT=0 
	,	@NullOverrideReceivedSentDate BIT=0 
)
	AS
	BEGIN
	------------------------------------------------------------------------------------------------------
	-- Stored procedure: [dbo].[gsp_B2BResultsUpdateByPK] 
	-- Author: DBA - Auto generated
	-- Version/Date: 1.0.003 / 2014-02-05 16:52:29.4183890 -05:00
	-- Purpose: Update table [dbo].[tblB2BResults]
	-- Notes:
	-- 1. @B2BResultGuid and @UpdatedBy are required parameter.
	-- 2. If a value other than NULL is passed on @_RowVersion parameter then the stored procedure verifies whether _RowVersion of the record matches with the  
	--    @_RowVersion parameter and it will throw an exception if they don't match, otherwise it saves the parameters regardless.
	-- 3. The @_RowVersion output parameter will always be updated with new timestamp generated by the updating of the record.
	-- 4. To update a column with NULL then set the corresponding "@NullOverride..." parameter to 1 and either pass NULL through the correlated parameter 
	--    or do not include the parameter at all. 
	--    Example - Saving NULL to SiteGuid on tblEquipment:
	--            EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...',@SiteGuid=NULL, @NullOverrideSiteGuid=1 
	--       or   EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...', @NullOverrideSiteGuid=1 
	------------------------------------------------------------------------------------------------------
	SET NOCOUNT ON;
	BEGIN TRY
		IF @_RowVersion IS NOT NULL AND NOT EXISTS(SELECT 1 FROM [dbo].[tblB2BResults] WHERE B2BResultGuid=@B2BResultGuid AND _RowVersion=@_RowVersion)
		BEGIN
			RAISERROR('Attempted to modify a stale copy of the record',16,1);
			RETURN;
		END
 
		UPDATE [dbo].[tblB2BResults] SET
			[TransID]=(CASE ISNULL(@NullOverrideTransID,0) WHEN 1 THEN @TransID ELSE ISNULL(@TransID,[TransID]) END)
		,	[Type]=(CASE ISNULL(@NullOverrideType,0) WHEN 1 THEN @Type ELSE ISNULL(@Type,[Type]) END)
		,	[Message]=(CASE ISNULL(@NullOverrideMessage,0) WHEN 1 THEN @Message ELSE ISNULL(@Message,[Message]) END)
		,	[DataError]=(CASE ISNULL(@NullOverrideDataError,0) WHEN 1 THEN @DataError ELSE ISNULL(@DataError,[DataError]) END)
		,	[ErrorStatus]=(CASE ISNULL(@NullOverrideErrorStatus,0) WHEN 1 THEN @ErrorStatus ELSE ISNULL(@ErrorStatus,[ErrorStatus]) END)
		,	[Disputed]=(CASE ISNULL(@NullOverrideDisputed,0) WHEN 1 THEN @Disputed ELSE ISNULL(@Disputed,[Disputed]) END)
		,	[Corrected]=(CASE ISNULL(@NullOverrideCorrected,0) WHEN 1 THEN @Corrected ELSE ISNULL(@Corrected,[Corrected]) END)
		,	[ReceivedSentDate]=(CASE ISNULL(@NullOverrideReceivedSentDate,0) WHEN 1 THEN @ReceivedSentDate ELSE ISNULL(@ReceivedSentDate,[ReceivedSentDate]) END)
		WHERE	B2BResultGuid=@B2BResultGuid;
 
		SELECT @_RowVersion=_RowVersion        
		FROM [dbo].[tblB2BResults]           
		WHERE B2BResultGuid=@B2BResultGuid;
	
 
	END TRY
	BEGIN CATCH        
		DECLARE	@_ErrMessage NVARCHAR(2048)      
				, @_ErrNumber INT           
				, @_ErrProcName NVARCHAR(126)           
				, @_ErrLineNumber INT;            
		SET @_ErrMessage = ERROR_MESSAGE();        
		SET @_ErrNumber = ERROR_NUMBER();        
		SET @_ErrProcName= ERROR_PROCEDURE();        
		SET @_ErrLineNumber = ERROR_LINE();            
		SET @_ErrMessage = 'Error: ' + @_ErrMessage + CHAR(13)+CHAR(10)                 
						+ 'Number: ' + CAST(@_ErrNumber AS VARCHAR(20)) + CHAR(13)+CHAR(10)                 
						+ 'Procedure Name: gsp_B2BResultsUpdateByPK' + CHAR(13)+CHAR(10)                  
						+ 'Line Number: ' + ISNULL(CAST(@_ErrLineNumber AS VARCHAR(20)),'') + CHAR(13)+CHAR(10);         
		RAISERROR(@_ErrMessage,18,1);      
	END CATCH    
END     
	
