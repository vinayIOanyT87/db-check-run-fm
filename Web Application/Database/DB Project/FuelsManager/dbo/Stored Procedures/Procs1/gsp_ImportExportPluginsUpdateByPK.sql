CREATE PROCEDURE [dbo].[gsp_ImportExportPluginsUpdateByPK]
(
		@ImportExportPluginGuid uniqueidentifier
	,	@PluginType nvarchar(50)=NULL
	,	@ConfigURL nvarchar(250)=NULL
	,	@RunURL nvarchar(250)=NULL
	,	@Import bit=NULL
	,	@Export bit=NULL
	,	@_RowVersion timestamp=NULL OUTPUT
	,	@NullOverridePluginType BIT=0 
	,	@NullOverrideConfigURL BIT=0 
	,	@NullOverrideRunURL BIT=0 
	,	@NullOverrideImport BIT=0 
	,	@NullOverrideExport BIT=0 
)
	AS
	BEGIN
	------------------------------------------------------------------------------------------------------
	-- Stored procedure: [dbo].[gsp_ImportExportPluginsUpdateByPK] 
	-- Author: DBA - Auto generated
	-- Version/Date: 1.0.003 / 2014-02-05 16:52:29.5444016 -05:00
	-- Purpose: Update table [dbo].[tblImportExportPlugins]
	-- Notes:
	-- 1. @ImportExportPluginGuid and @UpdatedBy are required parameter.
	-- 2. If a value other than NULL is passed on @_RowVersion parameter then the stored procedure verifies whether _RowVersion of the record matches with the  
	--    @_RowVersion parameter and it will throw an exception if they don't match, otherwise it saves the parameters regardless.
	-- 3. The @_RowVersion output parameter will always be updated with new timestamp generated by the updating of the record.
	-- 4. To update a column with NULL then set the corresponding "@NullOverride..." parameter to 1 and either pass NULL through the correlated parameter 
	--    or do not include the parameter at all. 
	--    Example - Saving NULL to SiteGuid on tblEquipment:
	--            EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...',@SiteGuid=NULL, @NullOverrideSiteGuid=1 
	--       or   EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...', @NullOverrideSiteGuid=1 
	------------------------------------------------------------------------------------------------------
	SET NOCOUNT ON;
	BEGIN TRY
		IF @_RowVersion IS NOT NULL AND NOT EXISTS(SELECT 1 FROM [dbo].[tblImportExportPlugins] WHERE ImportExportPluginGuid=@ImportExportPluginGuid AND _RowVersion=@_RowVersion)
		BEGIN
			RAISERROR('Attempted to modify a stale copy of the record',16,1);
			RETURN;
		END
 
		UPDATE [dbo].[tblImportExportPlugins] SET
			[PluginType]=(CASE ISNULL(@NullOverridePluginType,0) WHEN 1 THEN @PluginType ELSE ISNULL(@PluginType,[PluginType]) END)
		,	[ConfigURL]=(CASE ISNULL(@NullOverrideConfigURL,0) WHEN 1 THEN @ConfigURL ELSE ISNULL(@ConfigURL,[ConfigURL]) END)
		,	[RunURL]=(CASE ISNULL(@NullOverrideRunURL,0) WHEN 1 THEN @RunURL ELSE ISNULL(@RunURL,[RunURL]) END)
		,	[Import]=(CASE ISNULL(@NullOverrideImport,0) WHEN 1 THEN @Import ELSE ISNULL(@Import,[Import]) END)
		,	[Export]=(CASE ISNULL(@NullOverrideExport,0) WHEN 1 THEN @Export ELSE ISNULL(@Export,[Export]) END)
		WHERE	ImportExportPluginGuid=@ImportExportPluginGuid;
 
		SELECT @_RowVersion=_RowVersion        
		FROM [dbo].[tblImportExportPlugins]           
		WHERE ImportExportPluginGuid=@ImportExportPluginGuid;
	
 
	END TRY
	BEGIN CATCH        
		DECLARE	@_ErrMessage NVARCHAR(2048)      
				, @_ErrNumber INT           
				, @_ErrProcName NVARCHAR(126)           
				, @_ErrLineNumber INT;            
		SET @_ErrMessage = ERROR_MESSAGE();        
		SET @_ErrNumber = ERROR_NUMBER();        
		SET @_ErrProcName= ERROR_PROCEDURE();        
		SET @_ErrLineNumber = ERROR_LINE();            
		SET @_ErrMessage = 'Error: ' + @_ErrMessage + CHAR(13)+CHAR(10)                 
						+ 'Number: ' + CAST(@_ErrNumber AS VARCHAR(20)) + CHAR(13)+CHAR(10)                 
						+ 'Procedure Name: gsp_ImportExportPluginsUpdateByPK' + CHAR(13)+CHAR(10)                  
						+ 'Line Number: ' + ISNULL(CAST(@_ErrLineNumber AS VARCHAR(20)),'') + CHAR(13)+CHAR(10);         
		RAISERROR(@_ErrMessage,18,1);      
	END CATCH    
END     
	
