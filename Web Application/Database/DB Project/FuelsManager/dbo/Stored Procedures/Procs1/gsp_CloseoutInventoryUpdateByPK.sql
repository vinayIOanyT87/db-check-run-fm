CREATE PROCEDURE [dbo].[gsp_CloseoutInventoryUpdateByPK]
(
		@CloseoutInventoryGuid uniqueidentifier
	,	@Site nvarchar(30)=NULL
	,	@CloseoutDate date=NULL
	,	@ProductName nvarchar(30)=NULL
	,	@ManagerName nvarchar(100)=NULL
	,	@GrossBookInventory float=NULL
	,	@NetBookInventory float=NULL
	,	@GrossPhysicalInventory float=NULL
	,	@NetPhysicalInventory float=NULL
	,	@GrossVariance float=NULL
	,	@NetVariance float=NULL
	,	@UpdatedDate datetimeoffset(7)=NULL
	,	@UpdatedBy udtUserID=NULL
	,	@GrossBookPrice float=NULL
	,	@NetBookPrice float=NULL
	,	@GrossPhysicalPrice float=NULL
	,	@NetPhysicalPrice float=NULL
	,	@TransVersion bigint=NULL
	,	@MassBookInventory float=NULL
	,	@MassPhysicalInventory float=NULL
	,	@MassVariance float=NULL
	,	@MassBookPrice float=NULL
	,	@MassPhysicalPrice float=NULL
	,	@SiteGuid uniqueidentifier=NULL
	,	@ManagerCompanyGuid uniqueidentifier=NULL
	,	@ProductGuid uniqueidentifier=NULL
	,	@_RowVersion timestamp=NULL OUTPUT
	,	@NullOverrideSite BIT=0 
	,	@NullOverrideCloseoutDate BIT=0 
	,	@NullOverrideProductName BIT=0 
	,	@NullOverrideManagerName BIT=0 
	,	@NullOverrideGrossBookInventory BIT=0 
	,	@NullOverrideNetBookInventory BIT=0 
	,	@NullOverrideGrossPhysicalInventory BIT=0 
	,	@NullOverrideNetPhysicalInventory BIT=0 
	,	@NullOverrideGrossVariance BIT=0 
	,	@NullOverrideNetVariance BIT=0 
	,	@NullOverrideUpdatedDate BIT=0 
	,	@NullOverrideGrossBookPrice BIT=0 
	,	@NullOverrideNetBookPrice BIT=0 
	,	@NullOverrideGrossPhysicalPrice BIT=0 
	,	@NullOverrideNetPhysicalPrice BIT=0 
	,	@NullOverrideTransVersion BIT=0 
	,	@NullOverrideMassBookInventory BIT=0 
	,	@NullOverrideMassPhysicalInventory BIT=0 
	,	@NullOverrideMassVariance BIT=0 
	,	@NullOverrideMassBookPrice BIT=0 
	,	@NullOverrideMassPhysicalPrice BIT=0 
	,	@NullOverrideSiteGuid BIT=0 
	,	@NullOverrideManagerCompanyGuid BIT=0 
	,	@NullOverrideProductGuid BIT=0 
)
	AS
	BEGIN
	------------------------------------------------------------------------------------------------------
	-- Stored procedure: [dbo].[gsp_CloseoutInventoryUpdateByPK] 
	-- Author: DBA - Auto generated
	-- Version/Date: 1.0.003 / 2014-02-05 16:52:29.4313903 -05:00
	-- Purpose: Update table [dbo].[tblCloseoutInventory]
	-- Notes:
	-- 1. @CloseoutInventoryGuid and @UpdatedBy are required parameter.
	-- 2. If a value other than NULL is passed on @_RowVersion parameter then the stored procedure verifies whether _RowVersion of the record matches with the  
	--    @_RowVersion parameter and it will throw an exception if they don't match, otherwise it saves the parameters regardless.
	-- 3. The @_RowVersion output parameter will always be updated with new timestamp generated by the updating of the record.
	-- 4. To update a column with NULL then set the corresponding "@NullOverride..." parameter to 1 and either pass NULL through the correlated parameter 
	--    or do not include the parameter at all. 
	--    Example - Saving NULL to SiteGuid on tblEquipment:
	--            EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...',@SiteGuid=NULL, @NullOverrideSiteGuid=1 
	--       or   EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...', @NullOverrideSiteGuid=1 
	------------------------------------------------------------------------------------------------------
	SET NOCOUNT ON;
	BEGIN TRY
		IF @_RowVersion IS NOT NULL AND NOT EXISTS(SELECT 1 FROM [dbo].[tblCloseoutInventory] WHERE CloseoutInventoryGuid=@CloseoutInventoryGuid AND _RowVersion=@_RowVersion)
		BEGIN
			RAISERROR('Attempted to modify a stale copy of the record',16,1);
			RETURN;
		END
 
		UPDATE [dbo].[tblCloseoutInventory] SET
			[Site]=(CASE ISNULL(@NullOverrideSite,0) WHEN 1 THEN @Site ELSE ISNULL(@Site,[Site]) END)
		,	[CloseoutDate]=(CASE ISNULL(@NullOverrideCloseoutDate,0) WHEN 1 THEN @CloseoutDate ELSE ISNULL(@CloseoutDate,[CloseoutDate]) END)
		,	[ProductName]=(CASE ISNULL(@NullOverrideProductName,0) WHEN 1 THEN @ProductName ELSE ISNULL(@ProductName,[ProductName]) END)
		,	[ManagerName]=(CASE ISNULL(@NullOverrideManagerName,0) WHEN 1 THEN @ManagerName ELSE ISNULL(@ManagerName,[ManagerName]) END)
		,	[GrossBookInventory]=(CASE ISNULL(@NullOverrideGrossBookInventory,0) WHEN 1 THEN @GrossBookInventory ELSE ISNULL(@GrossBookInventory,[GrossBookInventory]) END)
		,	[NetBookInventory]=(CASE ISNULL(@NullOverrideNetBookInventory,0) WHEN 1 THEN @NetBookInventory ELSE ISNULL(@NetBookInventory,[NetBookInventory]) END)
		,	[GrossPhysicalInventory]=(CASE ISNULL(@NullOverrideGrossPhysicalInventory,0) WHEN 1 THEN @GrossPhysicalInventory ELSE ISNULL(@GrossPhysicalInventory,[GrossPhysicalInventory]) END)
		,	[NetPhysicalInventory]=(CASE ISNULL(@NullOverrideNetPhysicalInventory,0) WHEN 1 THEN @NetPhysicalInventory ELSE ISNULL(@NetPhysicalInventory,[NetPhysicalInventory]) END)
		,	[GrossVariance]=(CASE ISNULL(@NullOverrideGrossVariance,0) WHEN 1 THEN @GrossVariance ELSE ISNULL(@GrossVariance,[GrossVariance]) END)
		,	[NetVariance]=(CASE ISNULL(@NullOverrideNetVariance,0) WHEN 1 THEN @NetVariance ELSE ISNULL(@NetVariance,[NetVariance]) END)
		,	[UpdatedDate]=ISNULL(@UpdatedDate,SYSDATETIMEOFFSET())
		,	[UpdatedBy]= ISNULL(@UpdatedBy,SUSER_SNAME())
		,	[GrossBookPrice]=(CASE ISNULL(@NullOverrideGrossBookPrice,0) WHEN 1 THEN @GrossBookPrice ELSE ISNULL(@GrossBookPrice,[GrossBookPrice]) END)
		,	[NetBookPrice]=(CASE ISNULL(@NullOverrideNetBookPrice,0) WHEN 1 THEN @NetBookPrice ELSE ISNULL(@NetBookPrice,[NetBookPrice]) END)
		,	[GrossPhysicalPrice]=(CASE ISNULL(@NullOverrideGrossPhysicalPrice,0) WHEN 1 THEN @GrossPhysicalPrice ELSE ISNULL(@GrossPhysicalPrice,[GrossPhysicalPrice]) END)
		,	[NetPhysicalPrice]=(CASE ISNULL(@NullOverrideNetPhysicalPrice,0) WHEN 1 THEN @NetPhysicalPrice ELSE ISNULL(@NetPhysicalPrice,[NetPhysicalPrice]) END)
		,	[TransVersion]=(CASE ISNULL(@NullOverrideTransVersion,0) WHEN 1 THEN @TransVersion ELSE ISNULL(@TransVersion,[TransVersion]) END)
		,	[MassBookInventory]=(CASE ISNULL(@NullOverrideMassBookInventory,0) WHEN 1 THEN @MassBookInventory ELSE ISNULL(@MassBookInventory,[MassBookInventory]) END)
		,	[MassPhysicalInventory]=(CASE ISNULL(@NullOverrideMassPhysicalInventory,0) WHEN 1 THEN @MassPhysicalInventory ELSE ISNULL(@MassPhysicalInventory,[MassPhysicalInventory]) END)
		,	[MassVariance]=(CASE ISNULL(@NullOverrideMassVariance,0) WHEN 1 THEN @MassVariance ELSE ISNULL(@MassVariance,[MassVariance]) END)
		,	[MassBookPrice]=(CASE ISNULL(@NullOverrideMassBookPrice,0) WHEN 1 THEN @MassBookPrice ELSE ISNULL(@MassBookPrice,[MassBookPrice]) END)
		,	[MassPhysicalPrice]=(CASE ISNULL(@NullOverrideMassPhysicalPrice,0) WHEN 1 THEN @MassPhysicalPrice ELSE ISNULL(@MassPhysicalPrice,[MassPhysicalPrice]) END)
		,	[SiteGuid]=(CASE ISNULL(@NullOverrideSiteGuid,0) WHEN 1 THEN @SiteGuid ELSE ISNULL(@SiteGuid,[SiteGuid]) END)
		,	[ManagerCompanyGuid]=(CASE ISNULL(@NullOverrideManagerCompanyGuid,0) WHEN 1 THEN @ManagerCompanyGuid ELSE ISNULL(@ManagerCompanyGuid,[ManagerCompanyGuid]) END)
		,	[ProductGuid]=(CASE ISNULL(@NullOverrideProductGuid,0) WHEN 1 THEN @ProductGuid ELSE ISNULL(@ProductGuid,[ProductGuid]) END)
		WHERE	CloseoutInventoryGuid=@CloseoutInventoryGuid;
 
		SELECT @_RowVersion=_RowVersion        
		FROM [dbo].[tblCloseoutInventory]           
		WHERE CloseoutInventoryGuid=@CloseoutInventoryGuid;
	
 
	END TRY
	BEGIN CATCH        
		DECLARE	@_ErrMessage NVARCHAR(2048)      
				, @_ErrNumber INT           
				, @_ErrProcName NVARCHAR(126)           
				, @_ErrLineNumber INT;            
		SET @_ErrMessage = ERROR_MESSAGE();        
		SET @_ErrNumber = ERROR_NUMBER();        
		SET @_ErrProcName= ERROR_PROCEDURE();        
		SET @_ErrLineNumber = ERROR_LINE();            
		SET @_ErrMessage = 'Error: ' + @_ErrMessage + CHAR(13)+CHAR(10)                 
						+ 'Number: ' + CAST(@_ErrNumber AS VARCHAR(20)) + CHAR(13)+CHAR(10)                 
						+ 'Procedure Name: gsp_CloseoutInventoryUpdateByPK' + CHAR(13)+CHAR(10)                  
						+ 'Line Number: ' + ISNULL(CAST(@_ErrLineNumber AS VARCHAR(20)),'') + CHAR(13)+CHAR(10);         
		RAISERROR(@_ErrMessage,18,1);      
	END CATCH    
END     
	
