CREATE PROCEDURE [dbo].[gsp_EnterpriseQueueUpdateByPK]
(
		@EnterpriseQueueGuid uniqueidentifier
	,	@SourceType int=NULL
	,	@SourceID nvarchar(120)=NULL
	,	@DateAdded datetimeoffset(7)=NULL
	,	@Priority int=NULL
	,	@Status int=NULL
	,	@DateUpdated datetimeoffset(7)=NULL
	,	@UpdatedBy udtUserID=NULL
	,	@UpdatedDate datetimeoffset(7)=NULL
	,	@_RowVersion timestamp=NULL OUTPUT
	,	@NullOverrideSourceType BIT=0 
	,	@NullOverrideSourceID BIT=0 
	,	@NullOverrideDateAdded BIT=0 
	,	@NullOverridePriority BIT=0 
	,	@NullOverrideStatus BIT=0 
	,	@NullOverrideDateUpdated BIT=0 
	,	@NullOverrideUpdatedDate BIT=0 
)
	AS
	BEGIN
	------------------------------------------------------------------------------------------------------
	-- Stored procedure: [dbo].[gsp_EnterpriseQueueUpdateByPK] 
	-- Author: DBA - Auto generated
	-- Version/Date: 1.0.003 / 2014-02-05 16:52:29.4773949 -05:00
	-- Purpose: Update table [dbo].[tblEnterpriseQueue]
	-- Notes:
	-- 1. @EnterpriseQueueGuid and @UpdatedBy are required parameter.
	-- 2. If a value other than NULL is passed on @_RowVersion parameter then the stored procedure verifies whether _RowVersion of the record matches with the  
	--    @_RowVersion parameter and it will throw an exception if they don't match, otherwise it saves the parameters regardless.
	-- 3. The @_RowVersion output parameter will always be updated with new timestamp generated by the updating of the record.
	-- 4. To update a column with NULL then set the corresponding "@NullOverride..." parameter to 1 and either pass NULL through the correlated parameter 
	--    or do not include the parameter at all. 
	--    Example - Saving NULL to SiteGuid on tblEquipment:
	--            EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...',@SiteGuid=NULL, @NullOverrideSiteGuid=1 
	--       or   EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...', @NullOverrideSiteGuid=1 
	------------------------------------------------------------------------------------------------------
	SET NOCOUNT ON;
	BEGIN TRY
		IF @_RowVersion IS NOT NULL AND NOT EXISTS(SELECT 1 FROM [dbo].[tblEnterpriseQueue] WHERE EnterpriseQueueGuid=@EnterpriseQueueGuid AND _RowVersion=@_RowVersion)
		BEGIN
			RAISERROR('Attempted to modify a stale copy of the record',16,1);
			RETURN;
		END
 
		UPDATE [dbo].[tblEnterpriseQueue] SET
			[SourceType]=(CASE ISNULL(@NullOverrideSourceType,0) WHEN 1 THEN @SourceType ELSE ISNULL(@SourceType,[SourceType]) END)
		,	[SourceID]=(CASE ISNULL(@NullOverrideSourceID,0) WHEN 1 THEN @SourceID ELSE ISNULL(@SourceID,[SourceID]) END)
		,	[DateAdded]=(CASE ISNULL(@NullOverrideDateAdded,0) WHEN 1 THEN @DateAdded ELSE ISNULL(@DateAdded,[DateAdded]) END)
		,	[Priority]=(CASE ISNULL(@NullOverridePriority,0) WHEN 1 THEN @Priority ELSE ISNULL(@Priority,[Priority]) END)
		,	[Status]=(CASE ISNULL(@NullOverrideStatus,0) WHEN 1 THEN @Status ELSE ISNULL(@Status,[Status]) END)
		,	[DateUpdated]=(CASE ISNULL(@NullOverrideDateUpdated,0) WHEN 1 THEN @DateUpdated ELSE ISNULL(@DateUpdated,[DateUpdated]) END)
		,	[UpdatedBy]= ISNULL(@UpdatedBy,SUSER_SNAME())
		,	[UpdatedDate]=ISNULL(@UpdatedDate,SYSDATETIMEOFFSET())
		WHERE	EnterpriseQueueGuid=@EnterpriseQueueGuid;
 
		SELECT @_RowVersion=_RowVersion        
		FROM [dbo].[tblEnterpriseQueue]           
		WHERE EnterpriseQueueGuid=@EnterpriseQueueGuid;
	
 
	END TRY
	BEGIN CATCH        
		DECLARE	@_ErrMessage NVARCHAR(2048)      
				, @_ErrNumber INT           
				, @_ErrProcName NVARCHAR(126)           
				, @_ErrLineNumber INT;            
		SET @_ErrMessage = ERROR_MESSAGE();        
		SET @_ErrNumber = ERROR_NUMBER();        
		SET @_ErrProcName= ERROR_PROCEDURE();        
		SET @_ErrLineNumber = ERROR_LINE();            
		SET @_ErrMessage = 'Error: ' + @_ErrMessage + CHAR(13)+CHAR(10)                 
						+ 'Number: ' + CAST(@_ErrNumber AS VARCHAR(20)) + CHAR(13)+CHAR(10)                 
						+ 'Procedure Name: gsp_EnterpriseQueueUpdateByPK' + CHAR(13)+CHAR(10)                  
						+ 'Line Number: ' + ISNULL(CAST(@_ErrLineNumber AS VARCHAR(20)),'') + CHAR(13)+CHAR(10);         
		RAISERROR(@_ErrMessage,18,1);      
	END CATCH    
END     
	
