
	
CREATE PROCEDURE [dbo].[gsp_OpcUaSubscriptionUpdateByPK]
(
		@SubscriptionGuid uniqueidentifier
	,	@SessionGuid uniqueidentifier=NULL
	,	@TimeStamp datetimeoffset=NULL
	,	@PublishingInterval float=NULL
	,	@MaxLifetimeCount int=NULL
	,	@MaxKeepAliveCount int=NULL
	,	@MaxNotificationsPerPublish int=NULL
	,	@PublishingEnabled bit=NULL
	,	@Priority tinyint=NULL
	,	@PublishTimerExpiry bigint=NULL
	,	@KeepAliveCounter int=NULL
	,	@LifetimeCounter int=NULL
	,	@WaitingForPublish bit=NULL
	,	@LastSentMessage int=NULL
	,	@SequenceNumber bigint=NULL
	,	@MaxMessageCount int=NULL
	,	@RefreshInProgress bit=NULL
	,	@Expired bit=NULL
	,	@ModifyCount int=NULL
	,	@EnableCount int=NULL
	,	@DisableCount int=NULL
	,	@RepublishRequestCount int=NULL
	,	@RepublishMessageRequestCount int=NULL
	,	@RepublishMessageCount int=NULL
	,	@TransferRequestCount int=NULL
	,	@TransferredToAltClientCount int=NULL
	,	@TransferredToSameClientCount int=NULL
	,	@PublishRequestCount int=NULL
	,	@DataChangeNotificationsCount int=NULL
	,	@EventNotificationsCount int=NULL
	,	@NotificationsCount int=NULL
	,	@LatePublishRequestCount int=NULL
	,	@CurrentKeepAliveCount int=NULL
	,	@CurrentLifetimeCount int=NULL
	,	@UnacknowledgedMessageCount int=NULL
	,	@DiscardedMessageCount int=NULL
	,	@MonitoringQueueOverflowCount int=NULL
	,	@NextSequenceNumber int=NULL
	,	@EventQueueOverFlowCount int=NULL
	,	@SerializedSentMessages varbinary(max)=NULL
	,	@CreatedDate datetimeoffset(7)=NULL
	,	@CreatedBy udtUserID=NULL
	,	@UpdatedDate datetimeoffset(7)=NULL
	,	@UpdatedBy udtUserID=NULL
	,	@_ClusterIdx bigint=NULL
	,	@_RowVersion timestamp=NULL OUTPUT
	,	@NullOverrideSessionGuid BIT=0
	,  @NullOverrideTimeStamp BIT=0 
	,	@NullOverridePublishingInterval BIT=0 
	,	@NullOverrideMaxLifetimeCount BIT=0 
	,	@NullOverrideMaxKeepAliveCount BIT=0 
	,	@NullOverrideMaxNotificationsPerPublish BIT=0 
	,	@NullOverridePublishingEnabled BIT=0 
	,	@NullOverridePriority BIT=0 
	,	@NullOverridePublishTimerExpiry BIT=0 
	,	@NullOverrideKeepAliveCounter BIT=0 
	,	@NullOverrideLifetimeCounter BIT=0 
	,	@NullOverrideWaitingForPublish BIT=0 
	,	@NullOverrideLastSentMessage BIT=0 
	,	@NullOverrideSequenceNumber BIT=0 
	,	@NullOverrideMaxMessageCount BIT=0 
	,	@NullOverrideRefreshInProgress BIT=0 
	,	@NullOverrideExpired BIT=0 
	,	@NullOverrideModifyCount BIT=0 
	,	@NullOverrideEnableCount BIT=0 
	,	@NullOverrideDisableCount BIT=0 
	,	@NullOverrideRepublishRequestCount BIT=0 
	,	@NullOverrideRepublishMessageRequestCount BIT=0 
	,	@NullOverrideRepublishMessageCount BIT=0 
	,	@NullOverrideTransferRequestCount BIT=0 
	,	@NullOverrideTransferredToAltClientCount BIT=0 
	,	@NullOverrideTransferredToSameClientCount BIT=0 
	,	@NullOverridePublishRequestCount BIT=0 
	,	@NullOverrideDataChangeNotificationsCount BIT=0 
	,	@NullOverrideEventNotificationsCount BIT=0 
	,	@NullOverrideNotificationsCount BIT=0 
	,	@NullOverrideLatePublishRequestCount BIT=0 
	,	@NullOverrideCurrentKeepAliveCount BIT=0 
	,	@NullOverrideCurrentLifetimeCount BIT=0 
	,	@NullOverrideUnacknowledgedMessageCount BIT=0 
	,	@NullOverrideDiscardedMessageCount BIT=0 
	,	@NullOverrideMonitoringQueueOverflowCount BIT=0
	,	@NullOverrideNextSequenceNumber BIT=0 
	,	@NullOverrideEventQueueOverFlowCount BIT=0
	,	@NullOverrideSerializedSentMessages BIT=0 
	,	@NullOverrideCreatedDate BIT=0 
	,	@NullOverrideCreatedBy BIT=0 
	,	@NullOverrideUpdatedDate BIT=0 
	,	@NullOverride_ClusterIdx BIT=0 
)
	AS
	BEGIN
	------------------------------------------------------------------------------------------------------
	-- Stored procedure: [dbo].[gsp_OpcUaSubscriptionUpdateByPK] 
	-- Author: DBA - Auto generated
	-- Version/Date: 1.0.003 / 2015-11-07 13:01:32.3878792 -05:00
	-- Purpose: Update table [dbo].[tblOpcUaSubscription]
	-- Notes:
	-- 1. @SubscriptionGuid and @UpdatedBy are required parameter.
	-- 2. If a value other than NULL is passed on @_RowVersion parameter then the stored procedure verifies whether _RowVersion of the record matches with the  
	--    @_RowVersion parameter and it will throw an exception if they don't match, otherwise it saves the parameters regardless.
	-- 3. The @_RowVersion output parameter will always be updated with new timestamp generated by the updating of the record.
	-- 4. To update a column with NULL then set the corresponding "@NullOverride..." parameter to 1 and either pass NULL through the correlated parameter 
	--    or do not include the parameter at all. 
	--    Example - Saving NULL to SiteGuid on tblEquipment:
	--            EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...',@SiteGuid=NULL, @NullOverrideSiteGuid=1 
	--       or   EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...', @NullOverrideSiteGuid=1 
	------------------------------------------------------------------------------------------------------
	SET NOCOUNT ON;
	BEGIN TRY
		IF @_RowVersion IS NOT NULL AND NOT EXISTS(SELECT 1 FROM [dbo].[tblOpcUaSubscription] WHERE SubscriptionGuid=@SubscriptionGuid AND _RowVersion=@_RowVersion)
		BEGIN
			RAISERROR('Attempted to modify a stale copy of the record',16,1);
			RETURN;
		END
 
		UPDATE [dbo].[tblOpcUaSubscription] SET
			[SessionGuid]=(CASE ISNULL(@NullOverrideSessionGuid,0) WHEN 1 THEN @SessionGuid ELSE ISNULL(@SessionGuid,[SessionGuid]) END)
		,	[TimeStamp]=(CASE ISNULL(@NullOverrideTimeStamp,0) WHEN 1 THEN @TimeStamp ELSE ISNULL(@TimeStamp,[TimeStamp]) END)
		,	[PublishingInterval]=(CASE ISNULL(@NullOverridePublishingInterval,0) WHEN 1 THEN @PublishingInterval ELSE ISNULL(@PublishingInterval,[PublishingInterval]) END)
		,	[MaxLifetimeCount]=(CASE ISNULL(@NullOverrideMaxLifetimeCount,0) WHEN 1 THEN @MaxLifetimeCount ELSE ISNULL(@MaxLifetimeCount,[MaxLifetimeCount]) END)
		,	[MaxKeepAliveCount]=(CASE ISNULL(@NullOverrideMaxKeepAliveCount,0) WHEN 1 THEN @MaxKeepAliveCount ELSE ISNULL(@MaxKeepAliveCount,[MaxKeepAliveCount]) END)
		,	[MaxNotificationsPerPublish]=(CASE ISNULL(@NullOverrideMaxNotificationsPerPublish,0) WHEN 1 THEN @MaxNotificationsPerPublish ELSE ISNULL(@MaxNotificationsPerPublish,[MaxNotificationsPerPublish]) END)
		,	[PublishingEnabled]=(CASE ISNULL(@NullOverridePublishingEnabled,0) WHEN 1 THEN @PublishingEnabled ELSE ISNULL(@PublishingEnabled,[PublishingEnabled]) END)
		,	[Priority]=(CASE ISNULL(@NullOverridePriority,0) WHEN 1 THEN @Priority ELSE ISNULL(@Priority,[Priority]) END)
		,	[PublishTimerExpiry]=(CASE ISNULL(@NullOverridePublishTimerExpiry,0) WHEN 1 THEN @PublishTimerExpiry ELSE ISNULL(@PublishTimerExpiry,[PublishTimerExpiry]) END)
		,	[KeepAliveCounter]=(CASE ISNULL(@NullOverrideKeepAliveCounter,0) WHEN 1 THEN @KeepAliveCounter ELSE ISNULL(@KeepAliveCounter,[KeepAliveCounter]) END)
		,	[LifetimeCounter]=(CASE ISNULL(@NullOverrideLifetimeCounter,0) WHEN 1 THEN @LifetimeCounter ELSE ISNULL(@LifetimeCounter,[LifetimeCounter]) END)
		,	[WaitingForPublish]=(CASE ISNULL(@NullOverrideWaitingForPublish,0) WHEN 1 THEN @WaitingForPublish ELSE ISNULL(@WaitingForPublish,[WaitingForPublish]) END)
		,	[LastSentMessage]=(CASE ISNULL(@NullOverrideLastSentMessage,0) WHEN 1 THEN @LastSentMessage ELSE ISNULL(@LastSentMessage,[LastSentMessage]) END)
		,	[SequenceNumber]=(CASE ISNULL(@NullOverrideSequenceNumber,0) WHEN 1 THEN @SequenceNumber ELSE ISNULL(@SequenceNumber,[SequenceNumber]) END)
		,	[MaxMessageCount]=(CASE ISNULL(@NullOverrideMaxMessageCount,0) WHEN 1 THEN @MaxMessageCount ELSE ISNULL(@MaxMessageCount,[MaxMessageCount]) END)
		,	[RefreshInProgress]=(CASE ISNULL(@NullOverrideRefreshInProgress,0) WHEN 1 THEN @RefreshInProgress ELSE ISNULL(@RefreshInProgress,[RefreshInProgress]) END)
		,	[Expired]=(CASE ISNULL(@NullOverrideExpired,0) WHEN 1 THEN @Expired ELSE ISNULL(@Expired,[Expired]) END)
		,	[ModifyCount]=(CASE ISNULL(@NullOverrideModifyCount,0) WHEN 1 THEN @ModifyCount ELSE ISNULL(@ModifyCount,[ModifyCount]) END)
		,	[EnableCount]=(CASE ISNULL(@NullOverrideEnableCount,0) WHEN 1 THEN @EnableCount ELSE ISNULL(@EnableCount,[EnableCount]) END)
		,	[DisableCount]=(CASE ISNULL(@NullOverrideDisableCount,0) WHEN 1 THEN @DisableCount ELSE ISNULL(@DisableCount,[DisableCount]) END)
		,	[RepublishRequestCount]=(CASE ISNULL(@NullOverrideRepublishRequestCount,0) WHEN 1 THEN @RepublishRequestCount ELSE ISNULL(@RepublishRequestCount,[RepublishRequestCount]) END)
		,	[RepublishMessageRequestCount]=(CASE ISNULL(@NullOverrideRepublishMessageRequestCount,0) WHEN 1 THEN @RepublishMessageRequestCount ELSE ISNULL(@RepublishMessageRequestCount,[RepublishMessageRequestCount]) END)
		,	[RepublishMessageCount]=(CASE ISNULL(@NullOverrideRepublishMessageCount,0) WHEN 1 THEN @RepublishMessageCount ELSE ISNULL(@RepublishMessageCount,[RepublishMessageCount]) END)
		,	[TransferRequestCount]=(CASE ISNULL(@NullOverrideTransferRequestCount,0) WHEN 1 THEN @TransferRequestCount ELSE ISNULL(@TransferRequestCount,[TransferRequestCount]) END)
		,	[TransferredToAltClientCount]=(CASE ISNULL(@NullOverrideTransferredToAltClientCount,0) WHEN 1 THEN @TransferredToAltClientCount ELSE ISNULL(@TransferredToAltClientCount,[TransferredToAltClientCount]) END)
		,	[TransferredToSameClientCount]=(CASE ISNULL(@NullOverrideTransferredToSameClientCount,0) WHEN 1 THEN @TransferredToSameClientCount ELSE ISNULL(@TransferredToSameClientCount,[TransferredToSameClientCount]) END)
		,	[PublishRequestCount]=(CASE ISNULL(@NullOverridePublishRequestCount,0) WHEN 1 THEN @PublishRequestCount ELSE ISNULL(@PublishRequestCount,[PublishRequestCount]) END)
		,	[DataChangeNotificationsCount]=(CASE ISNULL(@NullOverrideDataChangeNotificationsCount,0) WHEN 1 THEN @DataChangeNotificationsCount ELSE ISNULL(@DataChangeNotificationsCount,[DataChangeNotificationsCount]) END)
		,	[EventNotificationsCount]=(CASE ISNULL(@NullOverrideEventNotificationsCount,0) WHEN 1 THEN @EventNotificationsCount ELSE ISNULL(@EventNotificationsCount,[EventNotificationsCount]) END)
		,	[NotificationsCount]=(CASE ISNULL(@NullOverrideNotificationsCount,0) WHEN 1 THEN @NotificationsCount ELSE ISNULL(@NotificationsCount,[NotificationsCount]) END)
		,	[LatePublishRequestCount]=(CASE ISNULL(@NullOverrideLatePublishRequestCount,0) WHEN 1 THEN @LatePublishRequestCount ELSE ISNULL(@LatePublishRequestCount,[LatePublishRequestCount]) END)
		,	[CurrentKeepAliveCount]=(CASE ISNULL(@NullOverrideCurrentKeepAliveCount,0) WHEN 1 THEN @CurrentKeepAliveCount ELSE ISNULL(@CurrentKeepAliveCount,[CurrentKeepAliveCount]) END)
		,	[CurrentLifetimeCount]=(CASE ISNULL(@NullOverrideCurrentLifetimeCount,0) WHEN 1 THEN @CurrentLifetimeCount ELSE ISNULL(@CurrentLifetimeCount,[CurrentLifetimeCount]) END)
		,	[UnacknowledgedMessageCount]=(CASE ISNULL(@NullOverrideUnacknowledgedMessageCount,0) WHEN 1 THEN @UnacknowledgedMessageCount ELSE ISNULL(@UnacknowledgedMessageCount,[UnacknowledgedMessageCount]) END)
		,	[DiscardedMessageCount]=(CASE ISNULL(@NullOverrideDiscardedMessageCount,0) WHEN 1 THEN @DiscardedMessageCount ELSE ISNULL(@DiscardedMessageCount,[DiscardedMessageCount]) END)
		,	[MonitoringQueueOverflowCount]=(CASE ISNULL(@NullOverrideMonitoringQueueOverflowCount,0) WHEN 1 THEN @MonitoringQueueOverflowCount ELSE ISNULL(@MonitoringQueueOverflowCount,[MonitoringQueueOverflowCount]) END)
		,	[NextSequenceNumber]=(CASE ISNULL(@NullOverrideNextSequenceNumber,0) WHEN 1 THEN @NextSequenceNumber ELSE ISNULL(@NextSequenceNumber,[NextSequenceNumber]) END)
		,	[EventQueueOverFlowCount]=(CASE ISNULL(@NullOverrideEventQueueOverFlowCount,0) WHEN 1 THEN @EventQueueOverFlowCount ELSE ISNULL(@EventQueueOverFlowCount,[EventQueueOverFlowCount]) END)
		,	[SerializedSentMessages]=(CASE ISNULL(@NullOverrideSerializedSentMessages,0) WHEN 1 THEN @SerializedSentMessages ELSE ISNULL(@SerializedSentMessages,[SerializedSentMessages]) END)
		,	[CreatedDate]=(CASE ISNULL(@NullOverrideCreatedDate,0) WHEN 1 THEN @CreatedDate ELSE ISNULL(@CreatedDate,[CreatedDate]) END)
		,	[CreatedBy]=(CASE ISNULL(@NullOverrideCreatedBy,0) WHEN 1 THEN @CreatedBy ELSE ISNULL(@CreatedBy,[CreatedBy]) END)
		,	[UpdatedDate]=ISNULL(@UpdatedDate,SYSDATETIMEOFFSET())
		,	[UpdatedBy]= ISNULL(@UpdatedBy,SUSER_SNAME())
		WHERE	SubscriptionGuid=@SubscriptionGuid;
 
		SELECT @_RowVersion=_RowVersion        
		FROM [dbo].[tblOpcUaSubscription]           
		WHERE SubscriptionGuid=@SubscriptionGuid;
	
 
	END TRY
	BEGIN CATCH        
		DECLARE	@_ErrMessage NVARCHAR(2048)      
				, @_ErrNumber INT           
				, @_ErrProcName NVARCHAR(126)           
				, @_ErrLineNumber INT;            
		SET @_ErrMessage = ERROR_MESSAGE();        
		SET @_ErrNumber = ERROR_NUMBER();        
		SET @_ErrProcName= ERROR_PROCEDURE();        
		SET @_ErrLineNumber = ERROR_LINE();            
		SET @_ErrMessage = 'Error: ' + @_ErrMessage + CHAR(13)+CHAR(10)                 
						+ 'Number: ' + CAST(@_ErrNumber AS VARCHAR(20)) + CHAR(13)+CHAR(10)                 
						+ 'Procedure Name: gsp_OpcUaSubscriptionUpdateByPK' + CHAR(13)+CHAR(10)                  
						+ 'Line Number: ' + ISNULL(CAST(@_ErrLineNumber AS VARCHAR(20)),'') + CHAR(13)+CHAR(10);         
		RAISERROR(@_ErrMessage,18,1);      
	END CATCH    
END     
	
GO
 
