CREATE PROCEDURE [map].[gsp_QualificationPersonLicenseToPersonUpdateByPK]
(
		@QualificationPersonLicenseToPersonGuid uniqueidentifier
	,	@QualificationGuid uniqueidentifier=NULL
	,	@PersonnelGuid uniqueidentifier=NULL
	,	@Sequence int=NULL
	,	@Instructor nvarchar(50)=NULL
	,	@DateCompleted datetimeoffset(7)=NULL
	,	@DateDue datetimeoffset(7)=NULL
	,	@ExpirationDate datetimeoffset(7)=NULL
	,	@ID varchar(25)=NULL
	,	@Rating nvarchar(20)=NULL
	,	@HistoricalRecord bit=NULL
	,	@UpdatedDate datetimeoffset(7)=NULL
	,	@UpdatedBy udtUserID=NULL
	,	@_RowVersion timestamp=NULL OUTPUT
	,	@NullOverrideQualificationGuid BIT=0 
	,	@NullOverridePersonnelGuid BIT=0 
	,	@NullOverrideSequence BIT=0 
	,	@NullOverrideInstructor BIT=0 
	,	@NullOverrideDateCompleted BIT=0 
	,	@NullOverrideDateDue BIT=0 
	,	@NullOverrideExpirationDate BIT=0 
	,	@NullOverrideID BIT=0 
	,	@NullOverrideRating BIT=0 
	,	@NullOverrideHistoricalRecord BIT=0 
	,	@NullOverrideUpdatedDate BIT=0 
)
	AS
	BEGIN
	------------------------------------------------------------------------------------------------------
	-- Stored procedure: [map].[gsp_QualificationPersonLicenseToPersonUpdateByPK] 
	-- Author: DBA - Auto generated
	-- Version/Date: 1.0.003 / 2014-02-05 16:52:30.1164588 -05:00
	-- Purpose: Update table [map].[tblQualificationPersonLicenseToPerson]
	-- Notes:
	-- 1. @QualificationPersonLicenseToPersonGuid and @UpdatedBy are required parameter.
	-- 2. If a value other than NULL is passed on @_RowVersion parameter then the stored procedure verifies whether _RowVersion of the record matches with the  
	--    @_RowVersion parameter and it will throw an exception if they don't match, otherwise it saves the parameters regardless.
	-- 3. The @_RowVersion output parameter will always be updated with new timestamp generated by the updating of the record.
	-- 4. To update a column with NULL then set the corresponding "@NullOverride..." parameter to 1 and either pass NULL through the correlated parameter 
	--    or do not include the parameter at all. 
	--    Example - Saving NULL to SiteGuid on tblEquipment:
	--            EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...',@SiteGuid=NULL, @NullOverrideSiteGuid=1 
	--       or   EXEC gsp_EquipmentUpdateByPK @EquipmentGuid='0000-...', @NullOverrideSiteGuid=1 
	------------------------------------------------------------------------------------------------------
	SET NOCOUNT ON;
	BEGIN TRY
		IF @_RowVersion IS NOT NULL AND NOT EXISTS(SELECT 1 FROM [map].[tblQualificationPersonLicenseToPerson] WHERE QualificationPersonLicenseToPersonGuid=@QualificationPersonLicenseToPersonGuid AND _RowVersion=@_RowVersion)
		BEGIN
			RAISERROR('Attempted to modify a stale copy of the record',16,1);
			RETURN;
		END
 
		UPDATE [map].[tblQualificationPersonLicenseToPerson] SET
			[QualificationGuid]=(CASE ISNULL(@NullOverrideQualificationGuid,0) WHEN 1 THEN @QualificationGuid ELSE ISNULL(@QualificationGuid,[QualificationGuid]) END)
		,	[PersonnelGuid]=(CASE ISNULL(@NullOverridePersonnelGuid,0) WHEN 1 THEN @PersonnelGuid ELSE ISNULL(@PersonnelGuid,[PersonnelGuid]) END)
		,	[Sequence]=(CASE ISNULL(@NullOverrideSequence,0) WHEN 1 THEN @Sequence ELSE ISNULL(@Sequence,[Sequence]) END)
		,	[Instructor]=(CASE ISNULL(@NullOverrideInstructor,0) WHEN 1 THEN @Instructor ELSE ISNULL(@Instructor,[Instructor]) END)
		,	[DateCompleted]=(CASE ISNULL(@NullOverrideDateCompleted,0) WHEN 1 THEN @DateCompleted ELSE ISNULL(@DateCompleted,[DateCompleted]) END)
		,	[DateDue]=(CASE ISNULL(@NullOverrideDateDue,0) WHEN 1 THEN @DateDue ELSE ISNULL(@DateDue,[DateDue]) END)
		,	[ExpirationDate]=(CASE ISNULL(@NullOverrideExpirationDate,0) WHEN 1 THEN @ExpirationDate ELSE ISNULL(@ExpirationDate,[ExpirationDate]) END)
		,	[ID]=(CASE ISNULL(@NullOverrideID,0) WHEN 1 THEN @ID ELSE ISNULL(@ID,[ID]) END)
		,	[Rating]=(CASE ISNULL(@NullOverrideRating,0) WHEN 1 THEN @Rating ELSE ISNULL(@Rating,[Rating]) END)
		,	[HistoricalRecord]=(CASE ISNULL(@NullOverrideHistoricalRecord,0) WHEN 1 THEN @HistoricalRecord ELSE ISNULL(@HistoricalRecord,[HistoricalRecord]) END)
		,	[UpdatedDate]=ISNULL(@UpdatedDate,SYSDATETIMEOFFSET())
		,	[UpdatedBy]= ISNULL(@UpdatedBy,SUSER_SNAME())
		WHERE	QualificationPersonLicenseToPersonGuid=@QualificationPersonLicenseToPersonGuid;
 
		SELECT @_RowVersion=_RowVersion        
		FROM [map].[tblQualificationPersonLicenseToPerson]           
		WHERE QualificationPersonLicenseToPersonGuid=@QualificationPersonLicenseToPersonGuid;
	
 
	END TRY
	BEGIN CATCH        
		DECLARE	@_ErrMessage NVARCHAR(2048)      
				, @_ErrNumber INT           
				, @_ErrProcName NVARCHAR(126)           
				, @_ErrLineNumber INT;            
		SET @_ErrMessage = ERROR_MESSAGE();        
		SET @_ErrNumber = ERROR_NUMBER();        
		SET @_ErrProcName= ERROR_PROCEDURE();        
		SET @_ErrLineNumber = ERROR_LINE();            
		SET @_ErrMessage = 'Error: ' + @_ErrMessage + CHAR(13)+CHAR(10)                 
						+ 'Number: ' + CAST(@_ErrNumber AS VARCHAR(20)) + CHAR(13)+CHAR(10)                 
						+ 'Procedure Name: gsp_QualificationPersonLicenseToPersonUpdateByPK' + CHAR(13)+CHAR(10)                  
						+ 'Line Number: ' + ISNULL(CAST(@_ErrLineNumber AS VARCHAR(20)),'') + CHAR(13)+CHAR(10);         
		RAISERROR(@_ErrMessage,18,1);      
	END CATCH    
END     
	
