CREATE TABLE [dbo].[tblHids] (
    [Index]     INT           IDENTITY (1, 1) NOT NULL,
    [ID]        NVARCHAR (50) NOT NULL,
    [PortIndex] INT           NULL,
    [DeviceID]  INT           NULL
);

